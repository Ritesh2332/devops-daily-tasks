# Day 07 - Docker build in CI

## Objective
Build docker image on runner without pushing to registry.

## Commands Used
See commands.sh

## Output
- Image built in workflow logs
- Local `docker build` works same way

## Learning
- build-push-action with push false
