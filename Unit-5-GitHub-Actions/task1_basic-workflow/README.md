# Day 01 - First GitHub Actions workflow

## Objective
Run a basic workflow on push to main.

## Note
Workflows must live at repo root: `.github/workflows/`. Copy `hello.yml` from this folder before pushing.

## Commands Used
See commands.sh

## Output
- Workflow shows green in Actions tab
- Logs show runner info + hello step

## Learning
- `on` defines triggers
- `jobs` / `steps` is where work happens
