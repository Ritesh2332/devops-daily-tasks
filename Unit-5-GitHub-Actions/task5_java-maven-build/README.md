# Day 05 - Java Maven build in CI

## Objective
Build and test Java app with Maven on ubuntu runner.

## Commands Used
See commands.sh

## Output
- `mvn test package` passes in Actions
- jar in target/

## Learning
- setup-java action handles JDK + maven cache
