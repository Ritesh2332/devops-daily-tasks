# Day 10 - Deploy workflow

## Objective
Chain build job then deploy job using github environment.

## Setup
Create environment named `staging` in repo settings.

## Commands Used
See commands.sh

## Output
- deploy job runs after build passes

## Learning
- needs links jobs
- environment can add approval gates
