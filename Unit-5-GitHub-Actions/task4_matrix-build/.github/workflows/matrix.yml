name: matrix-build

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  matrix-os:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
      - run: echo ${{ matrix.os }}
