# Day 02 - Freestyle job

## Objective
Create a freestyle project that runs shell commands on the controller.

## UI steps
1. New Item → name `hello-freestyle` → Freestyle project
2. Build → Execute shell → `echo "freestyle ok" && date`
3. Save → Build Now

## Commands Used
See commands.sh

## Output
- Console output shows echo + date

## Learning
- Freestyle = simplest job type, no Jenkinsfile in repo
