# Day 01 - Jenkins installation

## Objective
Run Jenkins in Docker and unlock the UI with the initial admin password.

## Commands Used
See commands.sh

## Output
- Jenkins UI on http://localhost:8080
- Setup wizard completed or skipped

## Learning
- Jenkins home is `/var/jenkins_home` in the container
- Port 8080 = UI, 50000 = agent communication
